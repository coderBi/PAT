#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_SIZE 20

int main()
{
    char arr[MAX_SIZE], ch;
    gets(arr);
    int i, length = strlen(arr);
    int n1, n2;

    if(length <= 0){
        return 0;
    }

    return 0;
}
