#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, m, k, q;
    scanf("%d %d %d %d", &n, &m, &k, &q);
    int arr[k], i;

    for(i = 0; i < k; i++){
        scanf("%d", &arr[i]);
    }
    return 0;
}
